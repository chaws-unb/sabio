#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "../ast.h"

void * eval_whileStatement(whileStatement * _whileStmt)
{
	//int expressionResult = (int)(*(double *)eval(ifStmt->expr));
	return 0;
}
